import { Landing } from "@/components/landing";

// Landing institucional — design importado do Claude Design (projeto
// "PlantiumAI Landing"). Substitui a home anterior para a Vercel refletir
// o design aprovado. Ver web/src/components/landing.tsx.
export default function LandingPage() {
  return <Landing />;
}
